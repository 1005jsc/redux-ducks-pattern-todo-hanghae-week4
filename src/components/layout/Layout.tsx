import React, { ReactNode } from 'react';
import styled from 'styled-components';

type LayoutProps = {
  children?: ReactNode;
};

const LayoutYo = ({ children }: LayoutProps) => {
  return <></>;
};
export default LayoutYo;
